{
  "framework": null,
  "outputDirectory": "frontend/dist",
  "installCommand": "rm -rf frontend/node_modules backend/node_modules && cd backend && npm install --include=dev && cd ../frontend && npm install --include=dev",
  "buildCommand": "cd frontend && npx --yes vite build && cd ../backend && npm run vercel-build",
  "functions": {
    "api/index.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/" }
  ]
}