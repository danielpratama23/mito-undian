{
  "framework": null,
  "outputDirectory": "frontend/dist",
  "installCommand": "cd backend && npm install --include=dev && cd ../frontend && npm install --include=dev",
  "buildCommand": "cd frontend && npx vite build && cd ../backend && npm run vercel-build",
  "functions": {
    "api/index.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/" }
  ]
}